export enum EnumTypeUser {
  paciente = 'PACIENTE',
  doctor = 'DOCTOR',
  admin = 'ADMIN',
}

export enum EnumStatusTip {
  PEDING = 'pendente',
  ACCEPTED = 'aceite',
  REJECTED = 'rejeitada',
}


export enum EnumEmojis {
  ALERT = '⚠️',
  CATION = '🚫',
  BOOKING = '🩺',
  MEDICATION = '💊',
  SYMPTOM = '🤒',
  OK = '✅'
}